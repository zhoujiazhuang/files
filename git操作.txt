git init  初始化本地库
git add 文件名    添加到暂存区
git commit -m 文件名      添加到本地库

git remote -v   查看远程库别名
git remote add 别名 远程库链接           创建别名

git push 别名或远程库链接 master          将分支推送到远程库

git pull 远程库名  master                            拉取分支到本地库

git clone 链接                                         克隆到本地